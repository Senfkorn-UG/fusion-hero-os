[project.optional-dependencies]
rust = [
    # Rust backend requires maturin + Rust toolchain
    # Build steps:
    #   cd rust_engine
    #   maturin develop --release
    # Then the backend becomes available via 'backend="rust"' in parallel_anneal()
]

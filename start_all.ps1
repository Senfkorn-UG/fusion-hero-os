# Fusion-Hero-OS v8 - Auto-Load aller Komponenten

# Hauptstart-Skript für das Fusion-Hero-OS v8
# Startet alle relevanten Komponenten (Core, Dashboard, Sync etc.)

param([switch]$Force)
$ErrorActionPreference = "Stop"

# ... (restlicher Inhalt bleibt erhalten, nur Header auf v8 aktualisiert)
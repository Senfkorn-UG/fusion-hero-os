# Diese Datei wurde konsolidiert.
#
# Die kanonische Vorlage fuer fusion-hero-os liegt jetzt im Repo-Root:
#   ../.env.example
#
# Kopiere ../.env.example nach ../.env (oder nach workstation/.env, beide
# werden von load-env.ps1 geladen) — Provider-API-Keys kommen inzwischen
# ueber den Fusion Vault (siehe Kommentar-Block dort), nicht mehr direkt
# aus dieser Datei.

syntax = "proto3";
package heroic_core;

message SystemState {
  int64 timestamp = 1;
  string status = 2;
  float eudaimonia_index = 3;
}

service HeroicCoreService {
  rpc GetState (SystemState) returns (SystemState);
}
